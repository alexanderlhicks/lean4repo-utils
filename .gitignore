# Python
__pycache__/
*.py[cod]

# Environments
.venv/
venv/

# Build artifacts
*.egg-info/
dist/
build/

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local agent config
.codex
.claude/settings.local.json

# Local planning / development-support docs (kept on the dev machine, not repo content
# while this is a solo effort — untracked via `git rm --cached`).
ROADMAP.md
SESSIONS.md
